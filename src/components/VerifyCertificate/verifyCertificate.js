import * as React from 'react';
import Layout from '../Layout/layout';

const drawerWidth = 240;
const navItems = ['Home', 'About', 'Contact'];

function verifyCertificate(props) {
 

  return (
   <>
   <Layout />
   </>
  );
}

export default verifyCertificate;